import {api} from "../../lib/api";
type Niche={id:string;name:string;slug:string}; type Product={id:string;name:string;slug:string};
export default async function Catalog(){const [niches,products]=await Promise.all([api<Niche[]>("/api/v1/catalog/niches"),api<Product[]>("/api/v1/catalog/products")]);return <><div className="eyebrow">Configuração sem VS Code</div><h1 className="title">Catálogo</h1><div className="grid"><div className="card"><h3>Nichos cadastrados</h3><div className="metric">{niches?.length??0}</div></div><div className="card"><h3>Produtos</h3><div className="metric">{products?.length??0}</div></div></div><section className="section card"><h2>Produtos</h2>{products?.length?<table className="table"><tbody>{products.map(p=><tr key={p.id}><td>{p.name}</td><td className="muted">{p.slug}</td></tr>)}</tbody></table>:<div className="empty">Rode o seed ou cadastre pela API.</div>}</section></>}
