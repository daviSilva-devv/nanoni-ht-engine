import {api} from "../../lib/api";
type Job={id:string;type:string;status:string;attempts:number;last_error:string|null};
export default async function Operations(){const jobs=await api<Job[]>("/api/v1/jobs");return <><div className="eyebrow">Operations Engine</div><h1 className="title">Operação</h1><div className="card"><div className="row"><div><h2>Jobs persistidos</h2><p className="muted">Falha vira estado e pode ser retomada; não some.</p></div><span className="pill">{jobs?.length??0}</span></div>{jobs?.length?<table className="table"><thead><tr><th>Tipo</th><th>Status</th><th>Tentativas</th><th>Erro</th></tr></thead><tbody>{jobs.map(j=><tr key={j.id}><td>{j.type}</td><td>{j.status}</td><td>{j.attempts}</td><td>{j.last_error??"—"}</td></tr>)}</tbody></table>:<div className="empty">Nenhum job ainda.</div>}</div></>}
